Read AGENTS.md before working on this repository or its local configuration.
Use `node scripts/cue-config.js status` to identify the active settings file.
Do not infer the running app's configuration from a sandboxed AppData copy.
